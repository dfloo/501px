import * as UploadUtil from '../util/upload_util';

export const RECEIVE_FORM_DATA = 'RECEIVE_FORM_DATA';

const receiveFormData = (formData) => ({
  type: RECEIVE_FORM_DATA,
  formData
});

export const upload = photo => (
)
